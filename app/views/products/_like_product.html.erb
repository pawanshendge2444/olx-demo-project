<hr><% like = Like.find_by(user_id: current_user.id , likeable_id: @product.id, likeable_type: "Product")%>
   <strong>Total Likes:</strong>&nbsp;
   <%= @product.likes.count %>
   <%if like.nil? %>
      <%= button_to "like", likes_path, params: {like: {likeable_id: @product.id, likeable_type: "Product", user_id: current_user.id }}, method: :post %>
   <% else %>
      <%= button_to "UnLike", like_path(like), method: :delete %>
   <% end %>